version: '3'
services:
  app:
    build: .
    ports:
      - "${APP_PORT}:${APP_PORT}"
    environment:
      - "APP_PORT=${APP_PORT}"
      - "APP_BASEURL=${APP_BASEURL}"
    volumes:
        - ./db:/microservice/db